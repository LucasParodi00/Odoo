<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extensión de la vista de res.partner -->
    <record id="view_partner_form_medical" model="ir.ui.view">
        <field name="name">res.partner.form.medical</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <!-- Agregar botones -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_appointments" 
                        type="object" 
                        class="oe_stat_button" 
                        icon="fa-calendar"
                        invisible="not is_patient">
                    <field name="appointment_count" widget="statinfo" string="Turnos"/>
                </button>
                <button name="action_view_clinical_history" 
                        type="object" 
                        class="oe_stat_button" 
                        icon="fa-file-text"
                        invisible="not is_patient">
                    <field name="clinical_history_count" widget="statinfo" string="HC"/>
                </button>
                <button name="action_view_animals" 
                        type="object" 
                        class="oe_stat_button" 
                        icon="fa-paw"
                        invisible="animal_count == 0">
                    <field name="animal_count" widget="statinfo" string="Animales"/>
                </button>
            </xpath>
            
            <!-- Agregar campo Es Paciente -->
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="is_patient"/>
            </xpath>
        </field>
    </record>

    <!-- Acción de menú para pacientes -->
    <record id="action_medical_patient" model="ir.actions.act_window">
        <field name="name">Pacientes</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="domain">[('is_patient', '=', True)]</field>
        <field name="context">{'default_is_patient': True, 'default_is_company': False}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Registrar un nuevo paciente
            </p>
            <p>
                Los pacientes son contactos que reciben atención médica.
            </p>
        </field>
    </record>

</odoo>
